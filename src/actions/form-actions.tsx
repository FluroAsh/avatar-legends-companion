"use server"

import { User } from "@clerk/nextjs/dist/types/server"

// eslint-disable-next-line
async function createCharacter(formData: FormData, user: User) {
  // TBC: Add this as a server action that can be called in the form component as a Server Action
}
